// === Operators ===
let a = 10;
let b = 5;
let c = 30;

// c = c + a;
// c = c - a;
// c = c * a;
// c = c / a;

// c += a
// c -= a
// c *= a
// c /= a

// c = 4 ** 2

console.log (c)

// === Data Types ===
const age = 29 // Number
const name = 'Alex' // String
const isProgrammer = true // Boolean
let x // undefined
        // null - отдельный тип данных

console.log(typeof name); // Проверить тип
console.log(typeof NaN)


// === Priotity ===

const fullAge = 29;
const birthYear = 1993
const currentYear = 2025
// > < ≥ ≤

const isFullAge = currentYear - birthYear >= fullAge
console.log(isFullAge)

const num1 = 42;
const num2 = '42'

console.log(num1 == num2)